function A(word, count) {
    if(callback){
        for (let i = 0; i < array.length; i++) {
            var result = "";
            result += word;
        }
        callback(result)
        return;
    }
    return result;
}

A("%", 5, function(result){
    console.log(`${result}`)
})